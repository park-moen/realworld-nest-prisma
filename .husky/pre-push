echo "🧪 Running tests before push..."

npm run test:pre-push

if [ $? -ne 0 ]; then
echo "❌ Tests failed! Cannot push to main."
echo "Fix the tests or use --no-verify to skip (not recommended)"
exit 1
fi

echo "✅ All tests passed! Pushing..."
