import './specs/signup.e2e-spec';
import './specs/login.e2e-spec';
import './specs/refresh.e2e-spec';

describe('Feature: 사용자 인증 E2E Tests', () => {
  it('should run all user authentication E2E tests', () => {
    expect(true).toBe(true);
  });
});
